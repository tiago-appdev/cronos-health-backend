{
  "name": "cronos-health-backend",
  "version": "1.0.0",
  "description": "Backend API for Cronos Health - Medical Appointment System",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "set NODE_OPTIONS=--experimental-vm-modules --no-warnings && jest",
    "test:watch": "set NODE_OPTIONS=--experimental-vm-modules --no-warnings && jest --watch",
    "test:coverage": "set NODE_OPTIONS=--experimental-vm-modules --no-warnings && jest --coverage",
    "seed": "node scripts/seed.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "pg": "^8.16.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/supertest": "^2.0.16",
    "jest": "^29.7.0",
    "nodemon": "^3.1.10",
    "supertest": "^6.3.4"
  }
}
